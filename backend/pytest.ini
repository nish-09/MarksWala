[pytest]
testpaths = tests
pythonpath = .
timeout = 300
markers =
    live_ai: uses the real Gemini API (needs GEMINI_API_KEY and quota)
    e2e: full end-to-end workflow with real Postgres/Redis/Qdrant/worker
filterwarnings =
    ignore::DeprecationWarning
